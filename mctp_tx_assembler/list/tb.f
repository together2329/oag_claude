# cocotb testbench: the TB is Python (tb/test_mctp_tx_assembler.py) driven by
# tb/Makefile (MODULE=test_mctp_tx_assembler, SIM=icarus). cocotb compiles the
# DUT itself via VERILOG_SOURCES, so there are no SystemVerilog TB sources to
# list here. This entry documents the cocotb TB module for tooling.
+incdir+tb
# tb/test_mctp_tx_assembler.py  (cocotb MODULE, not a Verilog compile source)
