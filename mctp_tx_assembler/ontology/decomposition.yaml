schema: oag_decomposition.v1
ip: mctp_tx_assembler
profile:
  mode: small_leaf_single_file
  rationale: Seed scaffold uses one leaf RTL file until complexity or reuse requires module decomposition.
  source: ontology/policies.yaml#structure_policy
modules:
  - id: mctp_tx_assembler
    name: mctp_tx_assembler
    role: top
    ownership: current_ip
    file: rtl/mctp_tx_assembler.v
    implementation_style: small_leaf_single_file
    owned_obligations: [OBL_TX_INTAKE, OBL_TX_FRAGMENT, OBL_TX_SOM_EOM, OBL_TX_SEQNUM, OBL_TX_HEADER, OBL_TX_SINGLEMSG, OBL_TX_ERR, OBL_TX_STATUS, OBL_TX_RESET]
    owned_contracts: [CONTRACT_TX_INTAKE, CONTRACT_TX_FRAGMENT, CONTRACT_TX_SOM_EOM, CONTRACT_TX_SEQNUM, CONTRACT_TX_HEADER, CONTRACT_TX_SINGLEMSG, CONTRACT_TX_ERR, CONTRACT_TX_STATUS, CONTRACT_TX_RESET]
    structure_refs: []
    source_refs: [ontology/requirements.yaml, ontology/obligations.yaml, ontology/contracts.yaml]
    edit_policy: editable
    notes: Replace this with greenfield_modular, legacy_preserve, or wrapper_adapter when the IP shape is known.
interfaces: []
legacy_sources: []
